{
  "name": "duperemover",
  "version": "1.0.0",
  "private": true,
  "description": "Finds '(1)' duplicate files within a directory and moves confirmed duplicates to a trash subfolder",
  "bin": {
    "duperemover": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
